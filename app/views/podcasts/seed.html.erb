<% @podcasts.each do |podcast| %>
  Podcast.create!(number: <%= podcast.number %>, title: %^<%= podcast.title %>^, description: %^<%= podcast.description %>^, date: %^<%= podcast.date %>^)<br/>
<% end %>